[pytest]
minversion = 7.0
addopts = -ra -q
testpaths = tests

markers =
    unit: testes unitários (sem banco, sem serviços externos)
    integration: testes de integração (banco, infra real)
    e2e: testes end-to-end
